package com.guavus.acume.cache.metadata

class MetadataLoader extends Runnable {

  
  override def run() = { 
    
    
  }
  
  def process(): Boolean = { 

    /*
     * 1. ORC and Parquet schema load.
     * 2. Lazy val init for org.apache.spark.sql.SchemaRdd class.
     * 3. Spark Sql Table for business cubes. 	 
     */
    true
  }
}